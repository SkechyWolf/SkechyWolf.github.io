import { getCustomProperty as e, incrementCustomProperty as t, setCustomProperty as o } from "./updateCustomProperty.js"; let SPEED = .04, groundElems = document.querySelectorAll("[data-ground]"); export function setupGround() { o(groundElems[0], "--left", 0), o(groundElems[1], "--left", 300) } export function updateGround(o, r) { groundElems.forEach(u => { t(u, "--left", -(o * r * .04)), -300 >= e(u, "--left") && t(u, "--left", 600) }) }